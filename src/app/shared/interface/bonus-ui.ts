export interface infoCards {
  headerClass: string;
  bodyClass: string;
  footerClass: string;
  headingClass: string;
  heading: string;
  title: string;
  desc: string;
}

export interface absoluteCard {
  bgColor: string;
  heading: string;
  img: string;
  text: string;
}
