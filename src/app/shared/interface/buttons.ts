export interface button {
  title: string;
  buttonClass: string;
  largeButton?: boolean;
}

export interface customOutline {
  title: string;
  middle?: boolean;
  left?: boolean;
  right?: boolean;
}
